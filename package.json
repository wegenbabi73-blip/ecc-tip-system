{
  "name": "ecc-tip-system-v8",
  "private": true,
  "version": "8.0.0",
  "scripts": {
    "install:all": "npm --prefix server install",
    "server": "npm --prefix server start",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  }
}
