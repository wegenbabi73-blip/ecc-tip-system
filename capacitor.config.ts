import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'et.gov.customs.ecc.tips',
  appName: 'ECC Tip System',
  webDir: 'www',
  server: { androidScheme: 'https' }
};
export default config;
